package com.soprahr.utils;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Date;

import net.minidev.json.JSONObject;

public class Utils {

	public Utils() {
		super();
	}

	public JSONObject dateEquals(Date d1, Date d2) {
		JSONObject jo = new JSONObject();
		Calendar c1 = Calendar.getInstance();
		Calendar c2 = Calendar.getInstance();
		c1.setTime(d1);
		c2.setTime(d2);

		if (c1.get(Calendar.YEAR) == c2.get(Calendar.YEAR)) {
			if (c1.get(Calendar.MONTH) == c2.get(Calendar.MONTH)) {
				if (c1.get(Calendar.DAY_OF_MONTH) == c2.get(Calendar.DAY_OF_MONTH)) {
					jo.put("Success", "Les dates sont égaux");
					return jo;
				} else {
					jo.put("Error", "Les mois sont differents");
					return jo;
				}
			} else {
				jo.put("Error", "Les mois sont differents");
				return jo;
			}
		} else {
			jo.put("Error", "Les années sont differentes");
			return jo;
		}

	}

	public void convertStringToDate(String sDate) {

		try {
			Date date1 = new SimpleDateFormat("dd/MM/yyyy").parse(sDate);
		} catch (ParseException e) {
			
			e.printStackTrace();
		}
	}
}
